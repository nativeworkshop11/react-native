import React from 'react';
import {ActivityIndicator, View} from 'react-native';

const Spinner = () => {
  return (
    <View>
      <ActivityIndicator />
    </View>
  );
};

export default Spinner;
